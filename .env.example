# Resend API key — sign up at https://resend.com, create an API key.
# Local dev: use a sandbox key (re_test_*). Production: real key in Vercel env.
RESEND_API_KEY=

# Sender address (joseveiga.dev verified in Resend).
#   QUOTE_FROM=contact@joseveiga.dev
QUOTE_FROM=

# Inbox that receives the briefs. Default: jveigacaminos@gmail.com.
# Swap to an alias like hire@joseveiga.dev once forwarding is set up.
QUOTE_RECIPIENT=
